## Why

<!-- What problem does this solve? Why should this change exist? Link to an issue if there is one. -->

## What changed

<!-- Brief description of the approach. What did you add/change/remove? -->

## Testing

<!-- How did you verify this works? Include the actual commands you ran and their output.
     For runtime changes, unit tests alone are not sufficient; exercise the real CLI/backend/VM path when possible.
     Example:
       $ ./bh run work echo hello
       hello
-->

**Environment:** <!-- OS, shell, backend URL or deployment, provider if relevant -->

## Checklist

- [ ] I ran `make clean && make build && make test`
- [ ] I tested the change end-to-end against a real backend/VM when runtime behavior changed
- [ ] I updated README.md (if adding/changing flags or config)
